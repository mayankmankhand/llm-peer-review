---
trigger: always_on
---
<!-- GENERATED by node .claude/scripts/build-layouts.js from AGENTS.md. Do not edit: change the source under .claude/ and rebuild. -->

Read `AGENTS.md` at the workspace root before doing anything else. It is the digest of the llm-peer-review toolkit's rules (the auto loop, the command table, what each stage does); follow it, and open `.claude/rules/toolkit.md` for anything it does not answer.

Everything under `.agents/` (the skills, the agents, this rule, `hooks.json`, `settings.toolkit.json`) is generated by `node .claude/scripts/build-layouts.js` from the source under `.claude/` and is never edited by hand or by an auto-fix. Change the source and rebuild.
