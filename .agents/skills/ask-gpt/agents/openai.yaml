# GENERATED by node .claude/scripts/build-layouts.js from .claude/commands/ask-gpt.md. Do not edit: change the source under .claude/ and rebuild.
# Codex reads only name and description from SKILL.md and may invoke a skill
# implicitly when a prompt matches its description. This stage is typed by a
# human only (M14), so implicit invocation is turned off here; $ask-gpt still works.
policy:
  allow_implicit_invocation: false
