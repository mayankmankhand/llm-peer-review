{
  "_generated": "GENERATED by node .claude/scripts/build-layouts.js from .claude/toolkit-permissions.json. Do not edit: change the source under .claude/ and rebuild.",
  "_note": "Antigravity does not read this file where it sits. Setup merges permissions.allow into ~/.gemini/antigravity-cli/settings.json, the per-machine file that applies to every project on the machine, replacing __WORKSPACE__ with this workspace's absolute path (no trailing slash) so the loop's file edits do not pause for a diff review. A committed file must never carry that path, which is why the placeholder exists; the documented relative form (write_file(src/)) does not say what it resolves against, so it is not used. toolPermission is left at its default, request-review.",
  "permissions": {
    "allow": [
      "command(git init)",
      "command(git add)",
      "command(git rm)",
      "command(git commit)",
      "command(git push)",
      "command(git pull)",
      "command(git fetch)",
      "command(git branch)",
      "command(git checkout)",
      "command(git stash)",
      "command(git worktree)",
      "command(git rev-parse)",
      "command(git rev-list)",
      "command(git status)",
      "command(git log)",
      "command(git diff)",
      "command(git show)",
      "command(git config)",
      "command(git remote add)",
      "command(git remote set-url)",
      "command(git check-ignore)",
      "command(gh repo create)",
      "command(gh repo view)",
      "command(gh repo edit)",
      "command(gh repo clone)",
      "command(gh auth status)",
      "command(gh issue create)",
      "command(gh issue view)",
      "command(gh issue close)",
      "command(gh issue list)",
      "command(gh issue reopen)",
      "command(gh label list)",
      "command(gh label create)",
      "command(gh pr create)",
      "command(gh pr view)",
      "command(gh pr diff)",
      "command(gh pr list)",
      "command(gh api)",
      "command(gh release list)",
      "command(npm install)",
      "command(npm uninstall)",
      "command(npm audit)",
      "command(npm outdated)",
      "command(node .claude/scripts/ask-gpt.js)",
      "command(node .claude/scripts/ask-gemini.js)",
      "command(node .claude/scripts/browse.js)",
      "command(node .claude/scripts/generate-index.js)",
      "command(node .claude/scripts/pre-push-check.js)",
      "command(node .claude/scripts/render-html.js)",
      "command(node .claude/scripts/session-init.js)",
      "command(node .claude/scripts/correction-ledger.js)",
      "command(node .claude/scripts/gen-media.js)",
      "command(node .claude/scripts/build-layouts.js)",
      "command(node .claude/scripts/write-guard.js)",
      "command(node .claude/scripts/chain-hook.js)",
      "command(bash .claude/scripts/open-artifact.sh)",
      "command(open)",
      "command(xdg-open)",
      "command(powershell.exe)",
      "command(explorer.exe)",
      "command(wslpath)",
      "command(bash scripts/setup/bump-version.sh)",
      "command(bash -n scripts/setup/setup.sh)",
      "command(bash -n scripts/setup/bump-version.sh)",
      "command(cp)",
      "command(ls)",
      "command(diff)",
      "command(echo)",
      "command(mkdir)",
      "command(cat)",
      "write_file(__WORKSPACE__/)"
    ]
  }
}
