# Copy to .env.local and fill in your API key.
# Never commit .env.local.

# Required for /dev-lead-gpt command (ChatGPT API calls)
OPENAI_API_KEY=

# Model version for ChatGPT (default: gpt-5.2)
# GPT_MODEL=gpt-5.2

# Required for /dev-lead-gemini command (Gemini API calls)
GEMINI_API_KEY=

# Model version for Gemini (default: gemini-3-flash-preview)
# GEMINI_MODEL=gemini-3-flash-preview

# Fallback mode: set to "1" to use concatenated prompts instead of systemInstruction
# Use this if you encounter issues with systemInstruction on your model/SDK version
# GEMINI_USE_CONCAT_PROMPT=1
