# GENERATED by node .claude/scripts/build-layouts.js from .claude/toolkit-permissions.json. Do not edit: change the source under .claude/ and rebuild.
# Starlark. Each prefix_rule lets one toolkit command prefix run outside the
# Codex sandbox without a prompt (decision "allow"), which the auto loop needs
# for git, gh, npm, the toolkit scripts and codex review. Codex reads this file
# only after the project is trusted once. Test a rule with:
#   codex execpolicy check --pretty --rules .codex/rules/toolkit.rules -- git status
# If a loop command still prompts, the documented wider fallback is
# network_access = true under [sandbox_workspace_write] in .codex/config.toml.

# Initializing repos, staging files, committing work
prefix_rule(pattern=["git", "init"], decision="allow", justification="Initializing repos, staging files, committing work")
prefix_rule(pattern=["git", "add"], decision="allow", justification="Initializing repos, staging files, committing work")
prefix_rule(pattern=["git", "rm"], decision="allow", justification="Initializing repos, staging files, committing work")
prefix_rule(pattern=["git", "commit"], decision="allow", justification="Initializing repos, staging files, committing work")

# Syncing with remote repositories
prefix_rule(pattern=["git", "push"], decision="allow", justification="Syncing with remote repositories")
prefix_rule(pattern=["git", "pull"], decision="allow", justification="Syncing with remote repositories")
prefix_rule(pattern=["git", "fetch"], decision="allow", justification="Syncing with remote repositories")

# Branch management and stashing work in progress
prefix_rule(pattern=["git", "branch"], decision="allow", justification="Branch management and stashing work in progress")
prefix_rule(pattern=["git", "checkout"], decision="allow", justification="Branch management and stashing work in progress")
prefix_rule(pattern=["git", "stash"], decision="allow", justification="Branch management and stashing work in progress")

# Creating, listing, and removing worktrees for parallel sessions
prefix_rule(pattern=["git", "worktree"], decision="allow", justification="Creating, listing, and removing worktrees for parallel sessions")

# Worktree detection, repo path queries, commit-range checks
prefix_rule(pattern=["git", "rev-parse"], decision="allow", justification="Worktree detection, repo path queries, commit-range checks")
prefix_rule(pattern=["git", "rev-list"], decision="allow", justification="Worktree detection, repo path queries, commit-range checks")

# Inspecting repo state and history
prefix_rule(pattern=["git", "status"], decision="allow", justification="Inspecting repo state and history")
prefix_rule(pattern=["git", "log"], decision="allow", justification="Inspecting repo state and history")
prefix_rule(pattern=["git", "diff"], decision="allow", justification="Inspecting repo state and history")
prefix_rule(pattern=["git", "show"], decision="allow", justification="Inspecting repo state and history")

# Git setup (safe.directory, remote URLs) and host detection via git config --get remote.origin.url
prefix_rule(pattern=["git", "config"], decision="allow", justification="Git setup (safe.directory, remote URLs) and host detection via git config --get remote.origin.url")
prefix_rule(pattern=["git", "remote", "add"], decision="allow", justification="Git setup (safe.directory, remote URLs) and host detection via git config --get remote.origin.url")
prefix_rule(pattern=["git", "remote", "set-url"], decision="allow", justification="Git setup (safe.directory, remote URLs) and host detection via git config --get remote.origin.url")

# Verifying .gitignore rules before committing
prefix_rule(pattern=["git", "check-ignore"], decision="allow", justification="Verifying .gitignore rules before committing")

# Repository scaffolding, viewing, cloning, and settings (GitHub)
prefix_rule(pattern=["gh", "repo", "create"], decision="allow", justification="Repository scaffolding, viewing, cloning, and settings (GitHub)")
prefix_rule(pattern=["gh", "repo", "view"], decision="allow", justification="Repository scaffolding, viewing, cloning, and settings (GitHub)")
prefix_rule(pattern=["gh", "repo", "edit"], decision="allow", justification="Repository scaffolding, viewing, cloning, and settings (GitHub)")
prefix_rule(pattern=["gh", "repo", "clone"], decision="allow", justification="Repository scaffolding, viewing, cloning, and settings (GitHub)")

# GitHub authentication status check (never gh auth token: it prints the token)
prefix_rule(pattern=["gh", "auth", "status"], decision="allow", justification="GitHub authentication status check (never gh auth token: it prints the token)")

# /create-issue and issue management (GitHub)
prefix_rule(pattern=["gh", "issue", "create"], decision="allow", justification="/create-issue and issue management (GitHub)")
prefix_rule(pattern=["gh", "issue", "view"], decision="allow", justification="/create-issue and issue management (GitHub)")
prefix_rule(pattern=["gh", "issue", "close"], decision="allow", justification="/create-issue and issue management (GitHub)")
prefix_rule(pattern=["gh", "issue", "list"], decision="allow", justification="/create-issue and issue management (GitHub)")
prefix_rule(pattern=["gh", "issue", "reopen"], decision="allow", justification="/create-issue and issue management (GitHub)")

# Managing GitHub labels
prefix_rule(pattern=["gh", "label", "list"], decision="allow", justification="Managing GitHub labels")
prefix_rule(pattern=["gh", "label", "create"], decision="allow", justification="Managing GitHub labels")

# Pull request workflows (GitHub); /document uses gh pr list for the cycle window
prefix_rule(pattern=["gh", "pr", "create"], decision="allow", justification="Pull request workflows (GitHub); /document uses gh pr list for the cycle window")
prefix_rule(pattern=["gh", "pr", "view"], decision="allow", justification="Pull request workflows (GitHub); /document uses gh pr list for the cycle window")
prefix_rule(pattern=["gh", "pr", "diff"], decision="allow", justification="Pull request workflows (GitHub); /document uses gh pr list for the cycle window")
prefix_rule(pattern=["gh", "pr", "list"], decision="allow", justification="Pull request workflows (GitHub); /document uses gh pr list for the cycle window")

# GitHub API calls and release checks; /review-deps queries dependency repos on every host
prefix_rule(pattern=["gh", "api"], decision="allow", justification="GitHub API calls and release checks; /review-deps queries dependency repos on every host")
prefix_rule(pattern=["gh", "release", "list"], decision="allow", justification="GitHub API calls and release checks; /review-deps queries dependency repos on every host")

# Managing dependencies
prefix_rule(pattern=["npm", "install"], decision="allow", justification="Managing dependencies")
prefix_rule(pattern=["npm", "uninstall"], decision="allow", justification="Managing dependencies")

# Dependency security and freshness checks (/review-deps)
prefix_rule(pattern=["npm", "audit"], decision="allow", justification="Dependency security and freshness checks (/review-deps)")
prefix_rule(pattern=["npm", "outdated"], decision="allow", justification="Dependency security and freshness checks (/review-deps)")

# Running the ask-gpt and ask-gemini debate scripts
prefix_rule(pattern=["node", ".claude/scripts/ask-gpt.js"], decision="allow", justification="Running the ask-gpt and ask-gemini debate scripts")
prefix_rule(pattern=["node", ".claude/scripts/ask-gemini.js"], decision="allow", justification="Running the ask-gpt and ask-gemini debate scripts")

# Running the headless browser QA script
prefix_rule(pattern=["node", ".claude/scripts/browse.js"], decision="allow", justification="Running the headless browser QA script")

# The codebase scanner behind /index
prefix_rule(pattern=["node", ".claude/scripts/generate-index.js"], decision="allow", justification="The codebase scanner behind /index")

# The M11 pre-push tripwire
prefix_rule(pattern=["node", ".claude/scripts/pre-push-check.js"], decision="allow", justification="The M11 pre-push tripwire")

# Rendering HTML artifacts and the artifact index modes
prefix_rule(pattern=["node", ".claude/scripts/render-html.js"], decision="allow", justification="Rendering HTML artifacts and the artifact index modes")

# The one-call session context every stage reads at startup
prefix_rule(pattern=["node", ".claude/scripts/session-init.js"], decision="allow", justification="The one-call session context every stage reads at startup")

# The correction ledger behind /document and /error-analysis
prefix_rule(pattern=["node", ".claude/scripts/correction-ledger.js"], decision="allow", justification="The correction ledger behind /document and /error-analysis")

# The design workflow helper (seeds, image, video, matte generation)
prefix_rule(pattern=["node", ".claude/scripts/gen-media.js"], decision="allow", justification="The design workflow helper (seeds, image, video, matte generation)")

# Building each tool's generated layout from .claude/ (issue #144)
prefix_rule(pattern=["node", ".claude/scripts/build-layouts.js"], decision="allow", justification="Building each tool's generated layout from .claude/ (issue #144)")

# The M2 write-guard around finders outside Claude Code (issue #144)
prefix_rule(pattern=["node", ".claude/scripts/write-guard.js"], decision="allow", justification="The M2 write-guard around finders outside Claude Code (issue #144)")

# The M14 chain-state hook helper outside Claude Code (issue #144)
prefix_rule(pattern=["node", ".claude/scripts/chain-hook.js"], decision="allow", justification="The M14 chain-state hook helper outside Claude Code (issue #144)")

# Opening a rendered artifact in the browser (the local viewport)
prefix_rule(pattern=["bash", ".claude/scripts/open-artifact.sh"], decision="allow", justification="Opening a rendered artifact in the browser (the local viewport)")

# The platform launchers open-artifact.sh reaches for
prefix_rule(pattern=["open"], decision="allow", justification="The platform launchers open-artifact.sh reaches for")
prefix_rule(pattern=["xdg-open"], decision="allow", justification="The platform launchers open-artifact.sh reaches for")
prefix_rule(pattern=["powershell.exe"], decision="allow", justification="The platform launchers open-artifact.sh reaches for")
prefix_rule(pattern=["explorer.exe"], decision="allow", justification="The platform launchers open-artifact.sh reaches for")
prefix_rule(pattern=["wslpath"], decision="allow", justification="The platform launchers open-artifact.sh reaches for")

# Running the version-bump script during release prep
prefix_rule(pattern=["bash", "scripts/setup/bump-version.sh"], decision="allow", justification="Running the version-bump script during release prep")

# Syntax-checking setup scripts before release
prefix_rule(pattern=["bash", "-n", "scripts/setup/setup.sh"], decision="allow", justification="Syntax-checking setup scripts before release")
prefix_rule(pattern=["bash", "-n", "scripts/setup/bump-version.sh"], decision="allow", justification="Syntax-checking setup scripts before release")

# Codex's built-in review as the code and security finder: a nested Codex that needs network, so it runs outside the sandbox (issue #144, /review on Codex)
prefix_rule(pattern=["codex", "review"], decision="allow", justification="Codex's built-in review as the code and security finder: a nested Codex that needs network, so it runs outside the sandbox (issue #144, /review on Codex)")

# Copying files (.env.local and CODEBASE_MAP.md into worktrees)
prefix_rule(pattern=["cp"], decision="allow", justification="Copying files (.env.local and CODEBASE_MAP.md into worktrees)")

# Reading directories, comparing files, writing output, creating folders
prefix_rule(pattern=["ls"], decision="allow", justification="Reading directories, comparing files, writing output, creating folders")
prefix_rule(pattern=["diff"], decision="allow", justification="Reading directories, comparing files, writing output, creating folders")
prefix_rule(pattern=["echo"], decision="allow", justification="Reading directories, comparing files, writing output, creating folders")
prefix_rule(pattern=["mkdir"], decision="allow", justification="Reading directories, comparing files, writing output, creating folders")
prefix_rule(pattern=["cat"], decision="allow", justification="Reading directories, comparing files, writing output, creating folders")
